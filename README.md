# Band Name Generator

**Description:**  

A simple Python program that generates a band name by combining the name of a city and a pet, based on user input.

**How to Run:**  
1. Clone the repository.  
2. Run the 'band_name_generator.py' script in a Python environment.  
3. Enter the required inputs when prompted.
